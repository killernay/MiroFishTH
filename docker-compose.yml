services:
  mirofish:
    build: .
    image: mirofishth:local
    container_name: mirofish
    env_file:
      - .env
    ports:
      - "${TAILSCALE_IP:?Set TAILSCALE_IP in .env}:${MIROFISH_PORT:-3410}:3000"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
